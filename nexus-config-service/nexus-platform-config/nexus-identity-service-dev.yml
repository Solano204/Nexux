spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/nexus_identity
    username: nexus
    password: nexus_dev_password
  data:
    redis:
      host: localhost
      port: 6380
      password: ""
  kafka:
    bootstrap-servers: localhost:19092

nexus:
  # Same dev default transaction-service uses for the same secret.
  plane-bridge-secret: dev-bridge-secret
