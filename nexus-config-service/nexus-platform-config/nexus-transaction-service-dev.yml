spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/nexus_transactions
    username: nexus
    password: nexus_dev_password
  elasticsearch:
    uris: http://localhost:9201
  kafka:
    streams:
      state-dir: /tmp/kafka-streams/transaction

nexus:
  plane-bridge-secret: dev-bridge-secret
  analytics:
    dynamodb:
      enabled: false   # set true only with real AWS credentials in dev
