spring:
  data:
    elasticsearch:
      uris: http://localhost:9200
    redis:
      host: localhost
      port: 6379
      password: ""
  kafka:
    streams:
      state-dir: /tmp/kafka-streams/analytics
