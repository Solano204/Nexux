# requirements.txt
# boto3/botocore come pre-installed in the Lambda Python runtime.
# Pinned here for local dev reproducibility.
boto3>=1.34.0
botocore>=1.34.0

# Image metadata inspection (size, format, corrupt-file detection)
# without downloading the full image from S3
Pillow>=10.3.0

# Structured JSON logging → CloudWatch Logs Insights queries
python-json-logger>=2.0.7