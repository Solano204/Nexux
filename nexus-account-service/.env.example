# .env.example — copy to .env and fill before running docker-compose.prod.yml
# NEVER commit .env to Git

POSTGRES_PASSWORD=change_me_strong_password
MONGODB_PASSWORD=change_me_strong_password
REDIS_PASSWORD=change_me_strong_password

OPENAI_API_KEY=sk-...     # Optional — account advisor only

GRAFANA_ADMIN_PASSWORD=change_me_strong_password

TRACING_SAMPLE_RATE=0.1
IMAGE_TAG=latest
DOCKER_HUB_USERNAME=nexusplatform
KAFKA_BOOTSTRAP_SERVERS=nexus-kafka:9092
