<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!--
        STANDALONE module — NO <parent> to nexus-financial-platform.
        Quarkus and Spring Boot cannot share a parent POM.
        Build separately: cd audit-write-native && mvn package [-Pnative]
        DO NOT add to root <modules>.
    -->

    <groupId>com.nexus</groupId>
    <artifactId>nexus-audit-write-native</artifactId>
    <version>1.0.0</version>
    <name>Nexus Audit Write Path — Quarkus Native</name>
    <description>
        High-throughput audit event ingestion.
        Quarkus GraalVM native binary: start 90ms, idle 28MB RSS.
        Subscribes to ALL 23 Kafka topics (fan-in).
        Writes to Elasticsearch with op_type=create (idempotent).
        Real-time compliance rule evaluation.
        Zero Spring dependencies — pure Quarkus reactive.
    </description>

    <properties>
        <!--
            FIX 1: Use io.quarkus:quarkus-bom (NOT io.quarkus.platform)
            for the 3.8.x line. The platform BOM only starts at 3.9+.
            3.8.6 is the latest LTS patch and IS in Maven Central.
        -->
        <quarkus.version>3.8.6</quarkus.version>
        <compiler-plugin.version>3.13.0</compiler-plugin.version>
        <!--
            FIX 2: Use java 21 — GraalVM CE native requires Java 21.
            Do NOT use Java 25 for this module.
        -->
        <java.version>21</java.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <quarkus.platform.artifact-id>quarkus-bom</quarkus.platform.artifact-id>
        <quarkus.platform.group-id>io.quarkus.platform</quarkus.platform.group-id>
        <quarkus.platform.version>${quarkus.version}</quarkus.platform.version>
        <surefire-plugin.version>3.1.2</surefire-plugin.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!--
                FIX 3: quarkus-rest and quarkus-messaging-kafka are NEW names
                introduced in Quarkus 3.9+. In 3.8.x the artifact IDs are:
                  quarkus-rest       → quarkus-resteasy-reactive
                  quarkus-messaging-kafka → quarkus-smallrye-reactive-messaging-kafka
                  quarkus-native     → not a standalone dep; handled by the plugin

                Using io.quarkus.platform:quarkus-bom which IS in Maven Central
                for 3.8.6 and manages all extension versions.
            -->
            <dependency>
                <groupId>io.quarkus.platform</groupId>
                <artifactId>quarkus-bom</artifactId>
                <version>${quarkus.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>

        <!-- QUARKUS CORE -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-arc</artifactId>
        </dependency>
        <!--
            FIX 4: quarkus-rest is the 3.9+ name.
            In 3.8.x use quarkus-resteasy-reactive (RESTEasy Reactive).
        -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-resteasy-reactive</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-resteasy-reactive-jackson</artifactId>
        </dependency>

        <!-- KAFKA — Reactive messaging for all 23 topics -->
        <!--
            FIX 5: quarkus-messaging-kafka is the 3.9+ name.
            In 3.8.x use quarkus-smallrye-reactive-messaging-kafka.
        -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-smallrye-reactive-messaging-kafka</artifactId>
        </dependency>

        <!-- ELASTICSEARCH — Write audit events -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-elasticsearch-java-client</artifactId>
        </dependency>

        <!-- MONGODB — Write compliance alerts -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-mongodb-client</artifactId>
        </dependency>

        <!-- REDIS — Velocity windows for compliance rules -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-redis-client</artifactId>
        </dependency>

        <!-- OBSERVABILITY -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-micrometer-registry-prometheus</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-opentelemetry</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-smallrye-health</artifactId>
        </dependency>

        <!--
            FIX 6: quarkus-native is NOT a dependency — it's not an artifact
            you add to dependencies. Native compilation is activated via the
            quarkus-maven-plugin with -Pnative. Remove it entirely.
        -->

        <!-- TESTING -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-junit5</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.rest-assured</groupId>
            <artifactId>rest-assured</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>io.quarkus.platform</groupId>
                <artifactId>quarkus-maven-plugin</artifactId>
                <version>${quarkus.version}</version>
                <extensions>true</extensions>
                <executions>
                    <execution>
                        <goals>
                            <goal>build</goal>
                            <goal>generate-code</goal>
                            <goal>generate-code-tests</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${compiler-plugin.version}</version>
                <configuration>
                    <release>${java.version}</release>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${surefire-plugin.version}</version>
                <configuration>
                    <systemPropertyVariables>
                        <java.util.logging.manager>
                            org.jboss.logmanager.LogManager
                        </java.util.logging.manager>
                        <maven.home>${maven.home}</maven.home>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <version>3.1.2</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                        <configuration>
                            <systemPropertyVariables>
                                <native.image.path>
                                    ${project.build.directory}/${project.build.finalName}-runner
                                </native.image.path>
                                <java.util.logging.manager>
                                    org.jboss.logmanager.LogManager
                                </java.util.logging.manager>
                                <maven.home>${maven.home}</maven.home>
                            </systemPropertyVariables>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>native</id>
            <activation>
                <property>
                    <name>native</name>
                </property>
            </activation>
            <properties>
                <skipITs>false</skipITs>
                <quarkus.native.enabled>true</quarkus.native.enabled>
            </properties>
        </profile>
    </profiles>

</project>