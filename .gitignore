.idea/
target/
*.jar

# Secrets — NEVER commit these
.env
secrets/
*.jks
*.p12
*.pem


# Terraform state — never commit this
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl

# Terraform Lambda build artifacts (pip installs + zips, generated on apply)
terraform/build/

# Variable files that may contain secrets
terraform.tfvars
terraform.tfvars.dev
*.auto.tfvars

# Crash logs
crash.log
crash.*.log

# Override files (local dev overrides)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Python bytecode cache (Lambda functions)
__pycache__/
*.pyc

# Terraform plan output (local artifacts, not source)
terraform/tfplan-*

# Local build/scratch artifacts
/terraform.zip
/README.html


